# xpire React app

This repository now contains a React + Vite app that converts the provided Scriptable Siri-safe expiry checker into browser-based, scriptable logic.

## Run locally

```bash
npm install
npm run dev
```

## Logic location

Core conversion logic is in `src/scriptableExpiry.js`.
